// import { StagedObject } from '@/api/networking/stagedobject/StagedObject';

describe('StagedData', () => {
  test('StagedObject catchup', () => {
    // const stagedObject = new StagedObject({
    //   id: '15325',
    //   points: 15,
    //   name: 'test',
    // });
    // const clientObject = stagedObject.clone();
    // stagedObject.set('id', '15326');
    // for (let i = 0; i < 5; i++) {
    //   stagedObject.set('points', stagedObject.get('points') + 1);
    // }
    // const changes = stagedObject.getChangesFrom(clientObject.getStep());
    // // clientObject.applyChanges(changes);
    // // expect(clientObject.get('id')).toBe('15326');
    // // expect(clientObject.get('points')).toBe(20);
  });
});
